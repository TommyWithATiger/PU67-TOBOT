pipeline:
  deploy:
    image: plugins/ssh
    host: ci.niklasmh.no
    username: tobot
    password: ${DEPLOY_PASSWORD}
    port: 22
    when:
      event: push, pull_request
      branch: fix/deploy/drone-support
    script:
      - echo "test" >> file.txt
      - make docker-prod
