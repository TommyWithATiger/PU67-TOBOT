pipeline:
  ssh:
    image: tobot/tobot-prod
    username: tobot
    port: 22
    when:
      event: push, pull_request
      branch: development
    commands:
      - echo pwd
      - make drone-image-prod
      - make drone-container-prod

  deployment:
    image: plugins/ssh
    host: ci.niklasmh.no
    username: tobot
    port: 22
    when:
      event: push
      branch: development
    script:
      - make drone-prod
