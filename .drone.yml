pipeline:
  ssh:
    image: Dockerfile.baseimage
    username: tobot
    port: 22
    when:
      event: push, pull_request
      branch: development
    commands:
      - echo pwd
      - make drone-image-prod
      - make drone-container-prod

  deployment:
    image: plugins/ssh
    host: ci.niklasmh.no
    username: tobot
    port: 22
    when:
      event: push
      branch: development
    script:
      - make drone-prod
