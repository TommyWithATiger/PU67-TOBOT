pipeline:
  ssh:
    image: plugins/ssh
    host: ci.niklasmh.no
    username: tobot
    password: ${DEPLOY_PASSWORD}
    port: 22
    when:
      event: push
      branch: development
    scripts:
      - cd /home/tobot
      - date >> file.txt
      - echo Testing ...
